---
apiVersion: v1
kind: Namespace
metadata:
  name: harbor
---
# harbor-helm-repo.yaml
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: harbor
  namespace: flux-system
spec:
  interval: 1h
  url: https://helm.goharbor.io
---
# harbor-release.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor
  namespace: harbor
spec:
  interval: 5m
  chart:
    spec:
      chart: harbor
      version: "1.14.0"  # Replace with latest stable version
      sourceRef:
        kind: HelmRepository
        name: harbor
        namespace: flux-system
  values:
    expose:
      type: ingress
      ingress:
        hosts:
          core: harbor.leclub.org.uk
        annotations:
          kubernetes.io/ingress.class: nginx
    externalURL: https://harbor.leclub.org.uk
    harborAdminPassword: "YourSecurePassword"